type: multiselect
options:
  type: query
  query: kirby.collection('boostidkvs')
  text: "{{ arrayItem.text }}"
  value: "{{ arrayItem.value }}"
search:
  # min: 1 # TODO: broken in K4 for arrayItems (aka Obj)
  display: 2
